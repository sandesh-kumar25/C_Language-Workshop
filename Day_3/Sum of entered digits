#include<stdio.h> //(WHILE LOOP)   // FOR SUM OF entered DIGITS
int main(){
int n,d,sum=0;
printf("Enter the value of n: ");
scanf("%d",&n);
while(n>0)
{ d=n%10;
  n=n/10;
  sum=sum+d;
}
printf("The sum is %d",sum);
return 0;
} 
